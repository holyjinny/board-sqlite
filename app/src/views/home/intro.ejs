<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>홈</title>
    <link rel="stylesheet" href="./css/home/intro.css">
</head>
<body>
    <section>
        <!-- 구름 -->
        <div class="clouds">
            <img src="./img/node.png" style="--i:1;">
            <img src="./img/github.png" style="--i:10;">
        </div>
        <div class="runway"></div>
        <img src="./img/airplane.png" class="plane">

        <!-- 문구 -->
        <div class="word">
            <% if(!user) { %>
                <p>좌클릭 꾹 누르면 간단한 애니메이션을 볼 수 있습니다.</p>
            <% } %>
        </div>
        
        <!-- 버튼 -->
        <div class="container">
            <% if(user) { %>
                <a href="/home"><span><%= user.username %></span></a>
            <% } else { %>
                <a href="/home"><span>입장하기</span></a>
            <% } %>
        </div>
    </section>
</body>
</html>